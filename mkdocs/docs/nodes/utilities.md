# Utilities Nodes

Utilities nodes are general-purpose helpers used across the MaStro node library. The most notable is the **Viewport Orientation** node, which exposes the current viewport rotation to Geometry Nodes so that text and annotation geometry can remain readable as the user rotates the 3D view.

*Additional nodes to be documented.*
