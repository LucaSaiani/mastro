# Properties — Camera Sets

The **Camera Sets** sub-panel of Project Data groups enabled cameras into named subsets for targeted batch calculations. It is visible only when at least one camera has projection enabled.

For full details on how Camera Sets work, see [2D Projection — Camera Sets](projection.md#camera-sets).
