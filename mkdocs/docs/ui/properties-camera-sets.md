# Properties — Camera Sets

The **Cameras** sub-panel of [Sets](properties-project-data.md) groups enabled cameras into named subsets for targeted batch calculations. It is visible only when at least one camera has projection enabled.

For full details on how Camera Sets work, including the membership checkbox's multi-row drag-toggle, see [2D Projection — Camera Sets](projection.md#camera-sets).
