# Properties — Building

A list of named buildings — sub-groupings within a block (e.g. *Tower 1*, *Podium*, *Annex*).

Each entry has a name. Use **+** to add, **×** to remove, and **↑ / ↓** to reorder entries.

Buildings are assigned to Mass and Block objects via the [Mass](sidebar-mass.md) sidebar panel.
